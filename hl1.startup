# =====================================================
# Host Hl1 - LAN LL (192.162.0.0/20)
# =====================================================

# IP y conexión
ip addr add 192.162.0.1/21 dev eth1
ip link set eth1 up

# Gateway
ip route add default via 192.162.15.254

# Para que funcione el nameserver
cd shared
cp resolv.conf /etc/
